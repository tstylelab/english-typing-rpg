# Work Log: 2026-06-23

## Purpose

Adjust the HP balance for the Eiken Grade 5 practice/listening path so that the user's child can continue progressing without making the later danger area feel suddenly trivial or disconnected.

## Work Done

- Created the work-log policy file at `docs/work-log/README.md`.
- Reviewed the current HP and battle tuning in `src/App.tsx`.
- Confirmed that listening practice uses the guide monster lane with the listening damage multiplier, so changing the global listening multiplier would make the whole mode too easy.
- Updated the Eiken Grade 5 guide HP curve instead of changing the listening damage multiplier.
- Changed the Grade 5 guide HP curve to:

```text
150, 170, 185, 200, 210, 220, 230, 240, 250, 260,
270, 285, 300, 315, 330, 345, 360, 370, 380, 400
```

- Updated the 20-stage monster builder so the 20th monster can use a separate final boss base HP from the 19th monster.

## Key Points

- The first suggestion to raise the listening damage multiplier from `0.28` was rejected because it would make the practice area too easy and create a sharper difficulty jump into the danger area.
- The chosen direction was to preserve the listening damage multiplier and tune only the Grade 5 guide HP curve.
- The final boss should remain at effective HP `800`.
- To keep that behavior, the 20th HP curve value remains `400`, and the existing final boss multiplier of `2` still produces `800`.
- The two monsters before the final boss felt too strong, so the 18th and 19th monsters were reduced to `370` and `380`.

## Main Files

- `docs/work-log/README.md`
- `docs/work-log/2026-06-23.md`
- `src/App.tsx`

## Verification

- Ran `npm.cmd run build`.
- Build succeeded.
- Vite reported the existing large chunk size warning, unrelated to this HP adjustment.

## Next Steps

- Have the child try the Eiken Grade 5 listening practice again around monsters 11 through 19.
- If the final stretch still feels too hard, tune only the late guide HP values before touching the listening damage multiplier.
