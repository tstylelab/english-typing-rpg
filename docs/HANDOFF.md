# Codex作業引き継ぎ

## 2026-09-24 英検1級①・② 初期コース追加（別PC引き継ぎ）

- 正式チェックアウト `english-typing-rpg-tstylelab` のmainで開始し、開始時はclean・origin/mainと一致。作業中のfetch後もorigin/mainとの差分なし。旧チェックアウトには触れていない。
- 本人提供の見出し2,400件を基に英検1級①（272単語／70熟語／40文）と②（262単語／82熟語／40文）を実装。訳・例文は本人から共有できないとの回答を受け、新規に作成。和訳だけでの出題を意識し、多義語・熟語は意味を限定した。
- 問題やHPの概要、厳選方針、検証、未検証事項は `docs/grade1-course-2026-09-24.md`。既存級の問題と保存データは変更していない。英検1級の初期値のみLevel1通常戦HPを420→1130、長文練習は短いミスに配慮。
- Grade1データ・元見出し照合、画面の教材選択/結果の文法表示/再読込/対人選択/画面幅、HP既存級回帰を検証。Grade2データ回帰、build、diff --check成功。lintエラー0・既存Hook警告1。実音声と人による勝率確認は未実施。
- 本人から別PC引き継ぎの依頼を受け、GitHubの `main` へ同期する。デスクトップ側では先に未コミット変更とブランチを確認し、作業ツリーが空で `main` が分岐していない場合のみ `git pull --ff-only origin main`。変更がある場合は破棄せず確認する。完了コミット番号はObsidianの当日セッション記録にも残す。

## 2026-09-24 英検2級30問追加（本人承認・commit/push対象）

- 提案した単語20語・熟語10表現を承認に基づき追加。現在549語／203表現／94文＝846問。前ターンの和訳69件・security例文修正も一緒に送信する。
- 追加データはsrc/data/questionSets/eiken/grade2Supplement.ts。独自の短い和訳・例文を作成。既存の3段階へ難易度を分け、共通データに統合。抽出元リストの採否記録と補強問題を分離しているため、元リスト用ジェネレーターで上書きされない。
- 旧問題の英語解答・準1級の共通語・HP・採点・保存形式は維持。解答数の増加に伴い学習対象の総数は30増える。
- Grade2テスト・build・lint（エラー0、既存警告1）成功。今回はデータ変更のみでUI/実音声の再試遊は未実施。全問題一覧更新、修正前後一覧はdocs/grade2-review-2026-09-24.md。
- 本人依頼によりmainへcommit/pushする。送信コミット番号・成功結果はObsidianのプロジェクト/2026-09-24セッションに記録。デスクトップ再開時は未コミット変更を保全し、fetch後に安全に同期する。

## 2026-09-24 英検2級の不足候補・和訳再点検（ローカル・未commit/push）

- 開始時 main=d94d0e5、clean。fetch後 origin/main と差分0。下記9/23の英検2級追加・訳点検はd94d0e5でpush済み（未push表記は当時の記録）。
- 和訳69件（L1=61、L2=5、L3=3）とsecurityの例文1件を修正。例文だけの状況を単語の意味に混ぜすぎた箇所を是正。生成元も修正し、全問題一覧を再生成。問題数529/193/94、他級・HP・採点・学習記録は不変。
- 追加は単語20＋熟語10の計30問を提案のみ。下位級で既習の熟語は除外し、準1級との共通215語は保持する案。本人承認前に追加しない。
- 全候補・理由・和訳69件の前後一覧：docs/grade2-review-2026-09-24.md。
- Grade2データテスト、build、lint（既存警告1・エラー0）、diff --check成功。今回UI/音声試遊は未実施。コミット・push依頼なしのため未実施。Obsidianにもプロジェクト/当日セッションを記録する。

## 2026-09-23 英検2級の和訳再点検（ローカル・未commit/push）

- 直下の英検2級追加作業に続き、Level 1=529語の訳と例文の意味・品詞、Level 2=193表現の似た訳、Level 3=94文の和訳と英文を再点検。準1級から再利用した訳も、2級側だけ例文の意味に絞った。
- 代表例：classic「時代を超えて親しまれる」→「長く親しまれている名作」、recover「失ったものを回復する」→「手術後に回復する」。熟語の同内容2問は片方の目的語を変更し、Level 3のprogramは「計画」→「プログラム」。詳細は `docs/grade2-course-2026-09-23.md`。問題数・採点・HP・旧級は不変更。
- 一文を和訳だけから完全再現する難しさは残る。実際の和訳プレイでの違和感は継続収集。全問題一覧を再生成済み。commit/pushは今回依頼されていないため保留。

## 2026-09-23 英検2級コース追加（ローカル・未commit/push）

- 本人提供の2級候補テキストを基に、正式チェックアウト `english-typing-rpg-tstylelab` で作業。開始時main=974e20e、clean、fetch後origin/mainと一致。古い隣接チェックアウトは操作していない。
- 元画面の3676問表示に対し、ユニークな見出しは単語1300・熟語400・英作文の型38、計1738。下位級との完全重複617を省き、Level 1=529語、Level 2=193表現、Level 3=94文（計816）を実装。訳・例文はゲーム用に編集／新規作成。候補の全見出しはリポジトリに転載しない。
- 既存準1級の訳・例文215語を2級候補として再利用し、特に多義で紛らわしい語は2級側の例文の意味に合わせて修正。5/4/3/準2級および準1級のデータは不変更。
- 3段階で易しい意味から拡張。共通コース選択・教材一覧・連続再生・20問バトル・保存復元へEiken2を追加。2級のみ準2級のHP・問題数・長文ミス減点を初期基準として再利用。実プレイ後に個別調整可能。
- 詳細は `docs/grade2-course-2026-09-23.md`、全問題 `docs/grade2-question-list.md`、選定集計/ID `docs/grade2-source-selection.json`。生成スクリプトは添付ファイルを明示引数で受け取るが、アプリの動作に添付は不要。
- Grade2データ/重複/文法/段階テスト、分離Chromeの12モード・画面幅・再読込・対人選択、build、lintを確認。人による音声・勝率確認は未実施。今回はpush指示なしなのでローカルのみ。デスクトップ側で作業する前に、本人の了承を得てcommit/pushし、向こうでfetch/pullすること。

## 2026-09-23 3級・準2級Level1実戦HP調整（未commit/push）

- 本人の「やってみてください」に基づき試算/実装。3級514語平均6.191文字、準2級650語平均6.714文字。9問ノーミス1スキップ、一定2字/秒、重複なし2万試行で7体目突破率は3級2.725→97.55%、準2級6.985→98.9%。実際の出題重み/入力変動を再現しない単純試算。
- 3級HP=380,450,520,590,660,730,800,850,900,950,1000,1050,1100,1140,1180,1220,1260,1300,1340。
- 準2級HP=400,480,560,640,710,780,850,900,950,1000,1040,1080,1120,1160,1200,1240,1280,1310,1340。
- 両級Level1音声/和訳バトル通常敵のみ。19体目1340・ボスHP/問題数・ミス半減・練習/弱点復習・Level2/3・他級は維持。保存データ不変。test-pre1-hp-curveの対象外回帰を拡張し成功。ローカル変更で未commit/push。

## 2026-09-22 英検4級Level 1実戦HP曲線

- 本人の条件付き承認で調査→調整。441語平均5.705文字。9問ノーミス1スキップ・一定2字/秒・2万回の重複なし抽出で7体目突破率0.21%→96.41%（HP1260→750）。実試遊の保証ではない。
- 音声/和訳バトル通常敵HP=350,420,490,560,630,690,750,810,870,930,990,1050,1100,1150,1200,1250,1290,1320,1340。終盤HPは維持し、序盤中盤だけ滑らかに。
- Level1のミス半減、速度計算、通常10問、ボスHP/問題数、練習、他級、習得判定は不変。4級L2/3の直前調整も維持。開始時3920762 clean、fetch差分なし。本人依頼に従い検証後commit/push。

## 2026-09-22 英検4級Level 2/3実戦HP・減点調整

- 本人依頼で試算→実装→push。対象は4級L2/3音声/和訳バトル。通常HP19段L2=1000,1070,1140,1220,1290,1360,1430,1510,1580,1650,1720,1790,1870,1940,2010,2080,2160,2230,2300。L3=1600,1720,1840,1970,2090,2210,2330,2460,2580,2700,2820,2940,3070,3190,3310,3430,3560,3680,3800。
- 平均文字数L2=16.535/L3=27.915（各200問）。重複なし10問抽出、5問各1ミス、一定1.6字/秒、2万試行：19体目旧0%/0%→新87.315%/95.61%。単純モデルで実試遊の保証ではない。
- ミス倍率max(L2:.75/L3:.8,1-ミス数/文字数)。準1級と共通関数getLongTextBattleMissMultiplier。4級ボス戦にも適用。通常10問・ボスHPと20/30/40/50問は維持。Level1/練習/他級の動作は維持。
- HP曲線/対象外/ボス/ミス境界テスト、build/diff-check成功。既存bundle警告あり。実試遊未確認。開始時8cf60c3 clean、fetch差分なし。

## 2026-09-22 準1級①② Level 2/3の実戦HP・ミス減点調整

- 本人の比較案承認に基づき音声/和訳バトル通常敵1〜19体のみ調整。L2=800,860,920,980,1040,1090,1150,1210,1260,1320,1370,1430,1480,1530,1590,1640,1690,1750,1800。L3=1800,1930,2070,2200,2330,2470,2600,2730,2870,3000,3130,3270,3400,3530,3670,3800,3930,4070,4200。
- ミス時倍率をmax(L2:0.75/L3:0.8,1-ミス数/入力文字数)へ。実戦のボス戦にも適用。10文字1ミス=0.9、28文字1ミス=27/28。速度倍率・スキップは変更なし。
- 通常戦10問、ラスボス/裏ボスHP・20/30/40/50問は据置（別途未確定）。新しい減点によりボスの実効難易度は下がり得る。Level1/練習/他級/習得条件は不変。制覇時の習得率を保証しない。
- test-pre1-hp-curve成功（①②、全Level、曲線、ボスHP/問題数、練習不変、減点境界）、build/diff-check成功。既存bundle警告あり。実試遊は未確認。開始時fe35eda clean、fetch追加更新なし。本人依頼に従いcommit/pushする。
- 直前の準1級②Level1調整はfe35edaでpush済み。

## 2026-09-22 準1級②にもLevel 1バトルHP曲線を適用（未commit/push）

- 本人追加依頼で準1級②の音声/和訳バトル通常敵1〜19体を①と同じHPに統一。PRE1_LEVEL1_BATTLE_HP_CURVEを①②で共有。
- 練習、ラスボス/裏ボス、Level 2/3、他級、採点/習得判定は不変。①②両方の19値と対象外の不変テスト、build/diff-check成功。既存bundle警告のみ。
- 今回は変更依頼のみにつきローカル未commit/push。直前の①調整はaa0ea62でpush済み。

## 2026-09-22 準1級① Level 1バトルHP調整

- 今後の本人の理想：ラスボス/裏ボスを全て倒す頃に、対象コースの「学習中」「もう少し」がほぼなくなるバランス。今後の出題・習得・難易度設計の目標であり、今回のHP変更だけで達成保証するものではない。今回の追加仕様変更は行わない。
- 前回のAI相談7日間/15件はaa89956でpush済み（下記の未push記録は作業時点のもの）。今回開始時clean・fetch追加変更なし。
- 本人が比較案の実装/pushを承認。準1級① Level 1の音声/和訳バトル、通常敵1〜19体だけHPを500,600,700,800,900,980,1050,1120,1180,1240,1300,1360,1420,1480,1540,1600,1660,1720,1780へ変更。7体目1701→1050。
- ラスボス3618・裏ボス5427/7236/9045、問題数、ミス減点/速度計算、練習エリア、準1級②、他級/Levelは変更しない。共通getCourseBaseHpで表示と実戦を一致させる。
- scripts/test-pre1-hp-curve.mjsで全19体・両モード・対象外・ボス不変を検証。本人の実際の試遊は反映後。既存の撃破/学習記録は変更しない。

## 2026-09-22 準2級push完了・AI相談の期間と件数変更

- 本人の「まずはコミットプッシュ」に従い、準2級コースを5817823でmainへcommit/push済み。fetchで追加更新なしを確認。公開デプロイ完了は未確認。
- 続いてAI相談の初期対象期間を過去7日間へ変更。直近200問への手動切替は維持。
- コピーする候補と復習表を最大10→15件。結果画面では今回のミス最大3件＋以前の履歴で最大15件。重複除外・不足時に水増ししない仕組み・保存期間30日/1000問は変更なし。
- 単体テストで15件上限、優先順位、重複除外、7日範囲、少数データを確認。分離Chromeで100/1000問の履歴を使い、7日初期選択・15件コピー・期間切替・スマホ・コピー失敗時など成功。build/diff-check成功（既存bundleサイズ警告あり）。
- AI相談の変更と本記録はローカル未commit/push。準2級のpushと混同しない。確認先http://127.0.0.1:5178/。Obsidianプロジェクト/同日セッションにも記録。

## 2026-09-22 英検準2級コース追加（未commit/push）

- 開始時main=79d574e・clean、fetchで追加更新なし。本人承認は準2級追加と不要な重複の抑制。今回pushの明示依頼なし。
- Level 1=650、Level 2=223、Level 3=173、計1,046問。元の番号付き1,632項目から975を採用、独自文法/意見短文71を追加。元画面の1,675とは区別。
- 5/4/3級との重複を除外し、具体語→用途拡張→抽象語、短い会話→文法→意見を伝える文へ3段階化。650単語は既習の単語見出しを大量に再追加していない。旧会話のcertainlyは副詞として再学習。熟語/短文の部分的な復習は残す。
- pre2.ts/pre2Vocabulary.ts/pre2Phrases.ts/pre2Sentences.ts、pre2Balance.tsを新設。共通選択肢・教材一覧・連続再生・対人バトル・保存検証にEikenPre2を追加。Level 3全問に文法ポイント、会話の特殊用法は個別解説。
- 通常戦10/10/8問。L1は共通単語バトル基準、L2/3は3級と同じ長文配慮を準2級限定で設定。旧各級の教材・HP・採点・保存キー不変。
- 詳細とHP/試算/制約：docs/pre2-course-2026-09-22.md。全問題一覧：docs/pre2-question-list.md。全選定番号：docs/pre2-source-selection.json。
- データ・元番号・旧教材比較・実計算シミュレーション、新級12モード遷移、文法結果・再読込保持・対人開始・1366/820/390px表示の分離Chrome試験成功。Grade3/連続再生の回帰試験成功。build成功、lint既存警告1・エラー0。実音声/IME/人の学習負担は未検証。
- 練習L2/3の1字/秒・半分の問題1ミスで300/300クリア。L1は既存基準を継承し終盤は2字/秒の無ミスでも平均ダメージ不足の場面あり。実戦L2最終裏ボスは1字/秒では難しいが2字/秒で到達可能な試算。詳細は設計資料。
- 本人は http://127.0.0.1:5178/ → 教材を選ぶ → 英検準2級から確認。未commit/pushなので公開サイトは未反映。デスクトップ移行前に本人承認を受けpushし、dirty保護→fetch/ff-onlyで引き継ぐ。
- Obsidianの現VaultのCodex/Projects/English-Typing-RPG.mdとSessions/Codex/2026-09/2026-09-22_English-Typing-RPG.mdにも概要を保存する。

## 2026-09-22 3級の不足語句25項目追加（commit/push承認済み）

- 開始時2250e3d・clean、fetch後追加更新なし。提案済み単語15語・熟語/構文10項目を本人承認で追加。現在514/183/168、合計865問。
- 個々の英語・和訳・自作例文はdocs/grade3-question-list.md冒頭。既存840問はそのまま、3段階内の適切な位置へ自動整列。追加熟語sourceId=0は元資料外の新規問題。
- 既存問題行の2250e3d比較テスト、重複・意味・文字数・例文・文法・計算シミュレーション成功。build成功、lint既存警告1。HP・採点・他級・既存保存キーは変更なし。
- 今回は教材のみの差分なので直前に確認済みの全画面テストを繰り返さず、データとbuildを重点確認。確定コミットとpush結果はObsidianに記録。

## 2026-09-22 英検3級のcommit/push承認・追加候補の点検

- 本人から3級コース840問のcommit/push指示を受領。以下の未commit/pushは前作業時点の記録。
- 送信後、3級に不足する単語・熟語がないか5/4級の収録内容も照合して提案する。問題数を他級に揃える必要はない。追加実装はまだ承認されていない。
- 確定コミット・push結果と提案はObsidianのプロジェクト／同日セッションに追記する。

## 2026-09-22 英検3級コース追加（未commit/push）

- 開始時main=91cff64・clean、fetchでGitHub追加更新なし。正式作業先english-typing-rpg-tstylelab。今回commit/push指示なし、公開サイトは変更していない。
- 添付の番号付き1300項目を基にLevel 1=499、Level 2=173、Level 3=168、計840問を新規追加。元単語900のうち新規474を全採用、下位級重複426のうち25だけ復習採用（café/cafe正規化）。熟語173は具体例に変換、会話72＋独自文法短文96。訳・例文・文法メモは新規作成。
- 機械的な全重複排除ではない。下位級と同じ熟語10項目・短文1項目を文法への接続のため保持。初案の不要な熟語重複17項目は削除。選定の全番号はdocs/grade3-source-selection.json。
- 易しい意味・構造の3段階→同段階内の文字数順。基礎練習・リスニング練習はステージ1〜7/8〜14/15以降で範囲拡張。実戦は全範囲。既存出題バランス・個別除外を維持。
- 3級を教材選択・単語リスト・連続再生・対人バトル共通の選択肢へ追加。全168短文の文法ポイントは保存済み復習オブジェクトでもテキストから復元。
- 3級専用grade3Balance.tsでLevel 2/3のHP・問題数・長文ミス減点を定義。文が長いほど時間がかかることを考慮し、通常戦10/8問。裏ボスは強め。Level 1は4級基準。5/4/準1級の教材・数値・保存キー不変。
- 詳細・HPと問題数・計算仮定・制約：docs/grade3-course-2026-09-22.md。全840問の英語/日本語/例文/文法キー：docs/grade3-question-list.md。
- テスト：新規データ/旧教材バイト比較/選定対応/実Appのダメージ抽出シミュレーション成功。既存和訳・連続再生ナビ・出題バランス・AI相談テスト成功。分離Chromeで教材選択・結果の1366/820/390px横はみ出しなし、12通りのレベル×モード遷移、短文正解→文法付き結果、更新後の3級保持、対人バトル開始、ブラウザエラー0。実音声はミュート、実機IME・人の学習負担は未検証。
- 最終build成功。lintエラー0・既存beginVersusTurn警告1。既存大容量bundle警告あり。diffチェック成功。
- ローカル http://127.0.0.1:5178/ → 教材を選ぶ → 英検3級。まず本人の試遊が次のステップ。デスクトップ移行には本人承認のcommit/pushが必要。未コミット変更を保護し、作業前にfetch/差分確認。
- Obsidianの現Vault「基本2026-09-14」へ連携経由でプロジェクト・2026-09-22セッションを保存。作業は21日開始、22日完了。


## 2026-09-21 撃破集計・結果・移動UIのcommit/push承認

- 本人から上記3点のcommit/push指示を受領。下の未commit/pushは作業途中の状態。
- push前にGitHub追加更新なし、差分チェック成功を確認。直前の表示・操作・build検証は無駄に繰り返さない。
- 確定コミットと送信結果はObsidian同日セッションに追記。デスクトップでは未コミット変更を保護してからff-only同期する。

## 2026-09-21 撃破集計・今日の解答数・バトル内移動（未commit/push）

- 開始時eff3df7、clean、fetch後リモート追加更新なし。
- 本人訂正：トップの撃破数は種類数ではなく、級・レベル・モード・出題方法ごとの攻略実績を別々に加算する。保存済みscopedキーをそのまま重複排除して数える。旧129種類の分母を撤去し「体」で表示。同一コースの同じ敵の再撃破は加算しない。保存データは変更なし。図鑑・称号の条件は今回変更なし。
- 結果画面左カラム先頭に今日の解答数を追加。トップと同じdailyActivityHistory（従来dailyProgressへのフォールバックあり）を共有。スキップを除く当日合計。
- 通常バトルの上端EXITバーを撤去。HP段左にトップに戻る／コース選択に戻る、右に既存ガイド切替・苦手語残数を移動。狭い画面は折り返す。ガイド等の機能は削除しない。
- 戻る処理は音・読み上げ・遅延勝利遷移を停止し、級/レベル選択や学習記録を保持。モンスターの上端が切れない余白を調整。
- scripts/test-battle-summary-navigation.mjsで同じ敵の級・レベル・出題方法別4件加算/重複1件除外、1366/820/390pxのHP左配置・横はみ出しなし、ガイド付き画面、両移動先、正解1問後7→8問でトップ/結果一致・スキップ除外、ブラウザエラー0を確認。分離無音ブラウザ、本人データ不使用。
- build成功。lintエラー0・既存警告1。今回はcommit/push指示なしのためローカル変更のまま。確認URL http://127.0.0.1:5178/ 。デスクトップで続ける前に本人承認のうえpushが必要。

## 2026-09-21 英検4級Level 1〜3の和訳改善（commit/push承認済み）

- 開始時main=d1816c2、clean、fetch後GitHub追加更新なし。
- 全841問（441/200/200）を点検、Level 1=21、Level 2=12、Level 3=15の計48件を修正。良い訳は維持し、短い補足と意味のずれの訂正に限定。
- 全件比較と残る制約は `docs/grade4-meaning-review-2026-09-21.md`。paint/draw、excited/exciting、until、remember to、丁寧な依頼、need等を改善。
- `grade4MeaningCorrections.json`をgetQuestionMeaningへ追加。元translation・保存キー・英語・例文・文法ポイント・HP・採点・問題数は不変。5級・準1級の先行修正も維持。AI相談の旧訳扱いは前回同様。
- データ互換性・連続再生テスト、build成功。lintエラー0・既存警告1。分離Chromeで1366/390pxの全変更表示・旧キー保存状態保持・代表行の横はみ出しなし、ブラウザエラー0。
- 本人からcommit/push明示承認済み。確定ハッシュとpush結果はObsidian同日セッションへ記録。公開デプロイ完了とpushは区別する。
- デスクトップ再開時はdirty状態保護→fetch・差分確認→ff-only同期。この先頭と変更一覧を読む。

## 2026-09-21 英検5級Level 1〜3の和訳改善（commit/push承認済み）

- 開始時main=2291cd7、clean、fetch後リモート追加更新なし。
- 全901問（501/200/200）を点検。Level 1=14、Level 2=15、Level 3=11の計40件を変更。良い訳は維持し、対象・場面・意味のずれに絞る。長文化しない本人方針を優先。
- 全40件の比較一覧、訳の長さ、残る別解の制約は `docs/grade5-meaning-review-2026-09-21.md`。
- `grade5MeaningCorrections.json`を既存getQuestionMeaningへ追加。元translation・英語・例文・保存キー・問題数・HP・採点・他級は不変。AI相談の旧訳の扱いも前回同様。
- データ互換性・連続再生テスト、build、diff成功。lintはエラー0・既存警告1。分離Chromeで1366/390pxの全40件表示・旧キー学習状態の保持・代表行の横はみ出しなし、ブラウザエラー0を確認。
- ユーザーからcommit/pushまで明示承認済み。確定ハッシュ・送信結果はObsidian同日セッションへ追記。公開反映はpushと区別する。
- デスクトップで再開時は未コミット変更を保護し、GitHubをfetchして差分確認後にff-only同期。このファイルの先頭と上記変更一覧を読む。

## 2026-09-21 準1級Level 2・3の和訳改善（commit/push承認済み）

- 開始時main=8fb3bba、clean、fetch後リモート追加更新なし。
- 準1級①②のLevel 2計315問・Level 3計150問を点検。Level 2=82問、Level 3=40問を修正。①は55/18件、②は27/22件。
- endear A to Bの好意の方向、let aloneの否定用法、add up toの訳などを訂正。熟語には対象・使う状況、文章には主語・時制・言い方の強さを補足。
- 全変更一覧・残る同義語や型出題の制約・参考辞書は `docs/pre1-level23-meaning-review-2026-09-21.md`。
- `pre1SentenceMeaningCorrections.json`を既存getQuestionMeaningへ追加。元translation・英語・例文・識別キーは不変。Level 1の85件、他級、HP、採点、問題数を維持。AI相談の旧訳の扱いも前回と同じ。
- データ/互換性テスト、連続再生テスト、build成功。lintはエラー0・既存警告1、diffチェック成功。
- 分離無音Chromeの `scripts/test-pre1-sentence-meaning-ui.mjs` で4コースの全122件表示、1366/390pxの代表的長文の横はみ出しなし、旧保存キーの手動学習状態をリロード後も保持、ブラウザエラー0を確認。
- 今回はユーザーからcommit/pushまで明示承認済み。確定ハッシュと送信結果はObsidian同日セッションに追記。公開反映はpushと区別する。

## 2026-09-21 和訳改善のcommit/push承認・報告方針

- ユーザーから85語の和訳改善のcommit/push承認を受領。下記の未commit/pushは作業途中の記録。確定コミット・送信結果はObsidianの同日セッションに記録。
- GitHub追加更新なし（HEADとorigin/main一致）を確認し、和訳・記録互換性テストとdiffチェックを再実行して成功。直前に成功したbuild・画面検証はコード変更がないため繰り返さない。
- 今後は用語修正の報告に「英語・変更前・変更後」の見比べられる一覧を必ず添える。多数の場合は主要例を回答内、全件をリンク先に掲載する。
- 確認は変更範囲とリスクに応じて必要最小限とし、同じ内容の再読込・検証を無駄に繰り返さない。

## 2026-09-21 準1級Level 1の和訳改善（ノートPC・未commit/push）

- 作業開始時clean。origin/mainの更新17件を確認し、28e0ad0→73e3ef3へfast-forward済み。デスクトップのAI相談・単語リスト・出題改善を保持。
- 準1級①・②Level 1の1,446問を和訳の曖昧さ・品詞の観点で点検。画像6語を含む85語の表示訳を改善。全問辞書照合・すべての類義語の一意化を保証するものではない。
- 詳細・全85件の前後比較・残る制約: `docs/pre1-meaning-review-2026-09-21.md`。
- `src/data/pre1MeaningCorrections.json`と`getQuestionMeaning`で出題、一覧、結果、連続再生の訳を統一。元のtranslationは学習状態・除外・選択・復習キューの識別に使うため変更禁止。AI相談履歴も集計キーを壊さないよう旧訳を維持し、コピーには旧訳が残る。
- 問題数・英語・例文・HP・採点・他の級/Levelは不変。85語の代表的用法を補足しており、ほかの用法や同義語を否定する定義ではない。
- `node scripts/test-question-meaning.mjs`、連続再生・出題バランス・AI相談コアテスト成功。build成功、lintエラー0・既存Hook警告1。diffチェック成功。
- 分離・無音Chromeの`test-question-meaning-ui.mjs`で6語を1366/820/390px表示、横はみ出しなし、旧キーで保存した学習状態の再読込保持、ブラウザエラー0を確認。初回テストでは段階表示と遅延描画への待機不足があり、テスト操作を修正して再実行成功。
- ローカル確認用Vite: http://127.0.0.1:5178/ 。今回はcommit/push指示なしのため公開未反映。次はユーザーの和訳モード試遊と、承認後のcommit/push。
- デスクトップで続けるには、この未コミット差分のpush完了後、先方でdirty状態を確認してからfast-forward同期。記録は現在VaultのObsidianにも保存。

## 2026-09-17 AI相談の末尾まとめを強化

- 復習表・個別Tipsに続く「まとめ：次の単語にも使える覚え方」を追加。候補を具体例に、パーツや関連語・綴りの目印がなぜ役立つか、どう応用するかを説明。先頭文で要点を示す見出し・箇条書き形式で、まとめだけは詳しくしてよい。共通点のこじつけ・ミス一覧・断定は避ける。
- 個別Tipsの件数制限なし・各1〜2文、反復非必須、候補選定と記録処理は不変。設定説明・生成文・コピーUIテストも更新。

## 2026-09-17 単語と和訳の視認性・幅配分修正

- 用語を大きく左端へ、和訳を隣へ配置。状態・選択・音声を補助領域へ分離し、PCの手動操作は例文右の余白を活用。スマホは折り返す。固定3:2配分・右寄せ和訳を撤去し、一覧とカードに幅とmin-widthを明示。
- 320/390/640/768/820/1024/1180/1366px、スクロール途中で1500→1180→900→768→390→1500pxへ変更するテストを追加。和訳がカードとoverflow祖先に収まること、選択・状態・復習・除外の操作を確認。代表カードはPC約93px。build成功。元画像の見切れの発生条件そのものは断定していない。

## 2026-09-17 Tips件数制限撤去（最新仕様）

- ユーザー指定により3〜5語・最大5語の制限を撤去。候補全語を対象に役立つ覚え方を求め、こじつけや的外れな説明で件数を埋めない。各1〜2文と反復ミス非必須は維持。復習表の候補最大10件・記録処理は変更なし。
- 設定画面説明と生成文・コピーUIテストも同じ仕様に更新。以下の古いTips件数指定は経緯であり現行仕様ではない。

## 2026-09-17 Tipsゼロになる条件を撤去（最新仕様）

- ユーザー承認により下記の単語リスト修正とまとめてmainへcommit/pushする。公開確認用の相談文は反映後に再コピーして使う。

- 前回の反復ミス必須・表だけで終了という条件が相談の目的を壊していたため撤去。依頼文を短く置換し、苦手語の覚え方を中心にする。表は最大10件、Tipsは3〜5語（候補3語未満なら全語）、各1〜2文。単発ミスも対象。
- データ側の「この語の個別Tipsは省略」も削除。反復R/L・A/E等は助言を調整する参考であって出力条件ではない。ミス報告の羅列・架空の語源は引き続き避ける。候補選定・記録・入力処理は変更なし。
- 全10語が単発かつ詳細記録なしでもTipsを求める回帰テストを追加。外部AIの実回答は未検証。直前の単語リスト修正も保持。

## 2026-09-17 単語リストをコンパクト化

- ページのPC最大幅を広げ、カード・一覧内側の余白を削減。単語と和訳はPCで左右、スマホで全幅の段組み。固定108pxの左インデントを撤去。
- 重複していた大型学習状態を削除し、状態は単語横に集約。自動判定は手動優先時だけ補足。例文の大きな枠をなくして短い行へ。例文・類義語・会話情報・手動操作は維持。
- 無音分離Chromeで1366／390／320pxの横はみ出し、代表行高さ（124／176／176px）、選択・手動学習状態・復習登録・除外を確認。100／1000問AI相談UI回帰テストも成功。ユーザーの成績・クリップボード不使用。

## 2026-09-17 ミス報告の再掲を廃止（この仕様を優先）

- 前回の「指摘だけでも可」と指摘例が不要なミス列をTipsへ移した原因。該当指示と例を削除し、回答全体で文字位置・誤入力矢印・回数報告を禁止。Tipsは反復根拠と役立つ具体的助言の両方がある場合のみ。なければ表だけで終了。
- 相談文のデータも変更。同じ語・位置・取り違えが別出題で2回以上の例だけ出力。全体集計も別出題2回以上だけ。単発語は復習表の候補には残すが、誤入力詳細を渡さない。保存やゲーム中の入力処理は不変。
- offensive／transplant／requirementの単発詳細非出力、同じ問題の複数位置を反復と誤認しない、同位置の異なる誤入力を合算しない、反復R/L・A/Eの根拠保持をテスト。外部AIの生成結果自体の遵守は保証できない。

## 2026-09-17 AI相談の表を復習用に変更

- 表は「英語｜和訳・類義語」の2列。ミス列をなくし、右セルを短くまとめ不要な改行を避ける。元のミスデータ・候補選定は維持。
- Tipsは最大5語、実際のミス箇所を短く指摘。自然に役立つ覚え方がある場合だけ添え、指摘だけで終了してよい。最低件数・最低文字数と記憶フック必須の指示を廃止。共通ヒントも有用かつ重複しない場合だけ。
- 完成例を指摘だけ／覚え方ありの2種類へ変更。やわらかい敬体・控えめな記号・正確性の注意は維持。設定dialogの説明とテストも更新。追ってユーザーからcommit/push承認済み。

## 2026-09-15 結果画面のAI相談欄を簡素化

- AI相談欄を移動・メニューの直下へ配置。通常表示は「AIにコピペで学習相談」の見出し＋相談3ボタン＋設定だけ。選定の説明と貼り付けの常設案内は設定dialogに残す。
- コピー後のステータス・外部リンク・エラー時の手動コピーは維持。プロンプト・候補選定・保存処理は変更なし。PC／390pxで表示順、初期ボタン4つ、説明段落なしをテスト。

## 2026-09-15 タメ口からやわらかい敬体へ

- Tips本文は「です・ます」を基本に、小学生にも大人にも自然な提案口調へ。「！」「♪」は個別Tips＋共通ヒント全体でそれぞれ1〜2か所程度、毎項目や表・注意書きには付けない。
- 完成例3件と暗記用の注意書きも敬体に修正し、旧タメ口の例を残さない。覚えるきっかけ→短い理由の構成、正確性・件数制約は維持。変更はプロンプト・テスト・文書のみ。

## 2026-09-15 Tipsを「覚えるきっかけ」から始める

- 語尾だけでなく構成を変更。Tipsは短い記憶フック見出し→短い理由。ミス位置・回数は表に任せて再説明しない。共通ミスのヒントも集計報告から始めない。
- temporary／barrierの視覚的イメージ、replayの関連語の完成例を追加。例は学習データではなく、候補への追加は禁止。幼児扱い・無理な語呂・架空の語源を避ける。
- 綴り・区切りの内部確認、同じ綴り同士の無意味な比較防止、別位置の取り違えを「入れ替え」と断定しない注意を追加。注意文自体で回答を埋めない。
- 変更はコピー用プロンプト・テスト・文書だけ。選定・保存・入力処理・UIは不変。文章品質は別AIに依存するため、公開後に新しい相談文をコピーし直して確認する。

## 2026-09-15 結果画面からAI相談・小学生にも読みやすいTips

- 通常のバトル結果にコピー／ChatGPT／Geminiの3ボタンを直接表示。設定はdialog、期間は双方で共有。コピー失敗時の手動対応・コピー後だけ外部を開く動作を共用。ボタンにフォーカス中のEnterで次のバトルへ飛ばないよう保護。
- 結果画面は直近バトルのミス語を最大3語、残りを今回ミスした語以外の期間内履歴から選び最大10語。重複なし、不足時は水増しなし。今も以前もミスした語を優先し、Tips用に選定元と双方のミス出題数を付記。単語リストは従来の履歴ランキング。
- 通常のinitBattleでセッションの参照集合をリセット。完了した記録への参照のみ保持し上限1000。保存形式・成績・出題方式は変更なし。集計はコピー時だけ。
- 小学生にも「ここ、迷うよね」「〜もアリだよ」の気楽な語り口へ。難しい漢字・専門語を日常語へ。幼児扱い・説教・無理な語呂・架空の語源は避ける。
- コアテスト、無音分離Chromeで実バトル結果／100・1000問／スマホ／Enter／コピー失敗を確認。build成功、lintは既存警告1件のみ。公開後の実機・別AIの文章品質は引き続き確認。

## 2026-09-15 AI相談Tipsの語り口改善

- ユーザーの「事務的で読みたくならない」「正解を言い直すだけでは役立たない」という指摘を反映。隣で一緒に覚えるような自然な話し言葉で、ミスした文字を思い出す手掛かりを求める。
- 小さなイメージ・語呂・関連語を歓迎。ただし無理なダジャレ、幼児向け口調、説教、正解の羅列は禁止。専門用語には短い説明を添える。文字数・2列表・取り違え集計・発音と暗記用読みの区別は維持。
- コピー用プロンプトとテストのみ変更。ゲーム中の処理・保存・UI構造は変更なし。ユーザーからcommit/push承認済み。

## 2026-09-15 意味・類義語と個別ミスに合ったTips

- 相談プロンプトを2列「単語・意味・類義語／私のミス」に変更。左セル内に改行して意味と類義語を併記し、列は増やさない。意味は既存データ、類義語はAIに意味・品詞の合う1語を依頼。不確かなものは省略。
- Tipsは再ミスの多い3〜5語・各2〜3文（目安80〜160文字）。実際のミス位置→文字の手掛かり→役立つ接頭辞／接尾辞や関連語の順。一般論で埋めない。
- 期間内の全記録から、文字の取り違えを出題方法別に最大5項目出力。位置数・出題数・異なる語数・正解文字の入力機会・最大2例を含める。共通ミスの助言は根拠がある最大2項目。記録のないL/R・a/eのミスを創作しない。
- 集計はコピー時のみ。保存形式・入力中の処理・独立dialog・AIを開くボタンは維持。ユーザーからcommit/push承認済み。確定コミットはObsidianへ記録。

## 2026-09-15 コピーしてAIを開くボタン

- 「コピーしてChatGPTを開く」「コピーしてGeminiを開く」をAI相談dialogへ追加。コピー成功後に固定URLを別タブで開く。貼り付け・送信は利用者が行う。
- 記録なし／コピー失敗時は外部を開かない。開けない場合のリンク常設（コピー後のみ）。noopener/noreferrerで開き、相談文や学習データをURLへ入れない。通常コピー・任意プレビュー・軽量化は維持。
- 100／1000問の無音UIテストで両URL・コピー先行・拒否時の手動対応・モバイル表示を確認。ユーザーからcommit/push承認済み。

## 2026-09-15 AI相談の実使用後フィードバック対応

- 約100問後の開く／コピーの重さ、AI回答が長すぎるとの報告。相談UIを独立dialogへ、全文プレビューは任意、開く時の保存なし、画面外単語カードの描画を省略。
- ユーザーと相談後、最大10件の「単語｜私のミス｜覚え方・理由」の表＋短いTipsに確定。理由60文字以内・Tips各1行50文字以内を原則に、実際の語構造と視覚的な区切りを区別。前置き・総評・重複説明不要。既存記録をそのまま利用。
- 候補は別出題での再ミスを優先し、出題回数、同位置の再ミス、複数位置の崩れを考慮。最大10件・各5例のデータ。文字別の長い全体集計は送らない。
- 100問の分離Chrome計測でコピー操作のTaskDuration差約45→9ms。ユーザー環境の強い遅延自体は未再現。100／1000問、PC／スマホ幅、コピー失敗、開閉・フォーカス、一覧末尾操作を確認。詳細は `docs/ai-study-review-trial.md`。
- 最終版は準1級20語・1000問の分離Chromeでコピーのハンドラ約1.2ms、TaskDuration差約10.4ms。100／1000問のUI回帰テストを `scripts/test-ai-study-review-ui.mjs` に追加（ローカルVite＋任意のPlaywrightが必要）。
- 今回の修正はユーザーからcommit/push承認済み。確定コミットはObsidianの同日セッションへ記録。公開後の実機で操作感と回答の短さを再確認する。

## 2026-09-15 AI学習相談の試作

- 単語リストに「AIに学習相談」を追加。直近200問／過去7日間の記録＋スペル暗記の工夫を求める依頼文をコピーできる。外部AIへの自動送信なし。
- 通常の練習／バトルの最初の英字入力を位置単位で記録。入力中はメモリのみ、保存は画面移動等の区切り。プレイヤー別・直近30日／最大1000問／JSON600,000文字上限。既存成績と別保存で、成績の転送対象には含めない。
- 記録OFF、相談用記録だけの削除、保存失敗表示、手動コピーを用意。既存の採点・出題・入力判定は変更していない。
- 詳細な仕様、測定、テストデータからのAI回答例は `docs/ai-study-review-trial.md`。新規テストは `node scripts/test-ai-study-review.mjs --sample`。
- 2026-09-15にユーザーから確認後のcommit/push承認あり。確定コミットはObsidianの同日セッションへ記録。実データを蓄積して、助言の有用性を次に確認する。

## 2026-09-09 終盤の未習得出題保証・モード選択の重なり修正

- 通常の練習／バトルで、教材・Levelの出題可能語（除外を除く）の80%以上が「覚えた」なら、10問区切りで最低3問を「学習中・もう少し」から出す。
- 3・6・9問目までに未習得を最低1・2・3問。通常抽選や期限到来の復習で出た未習得も数え、保証が不要な枠は既存の6:3:1の抽選／復習処理を維持。
- 保証枠の未習得は一巡するまで未出題を優先。残り1語にも対応し、全語習得時は解除。英検の基礎練習の段階別出題範囲と除外を維持。
- 問題選択時だけ集計。保存データ追加なし。プレイヤー・教材・Level・モード・入力方法ごとに管理し、各モンスター／再挑戦では10問枠をリセット、語の巡回は維持。再読み込みでは再スタート。
- 解答直後の昇格／降格を次の出題に反映。保証枠で延期した復習キューは消費しない。苦手だけの復習・任意選択復習・対戦・はじめてバトルには適用しない。
- 「今日の冒険を選ぼう！」を右上の固定配置からMode Selectの横へ移し、覚えたカードとの重なりを解消。遊び方にも保証の説明を追加。
- `node scripts/test-learning-question-balance.mjs`、連続再生テスト、build成功。lintは既存のbeginVersusTurn警告1件のみ。
- 無音の分離Chromeで残り5語の通常ゲーム10問に未習得3問＋習得済み7問を確認。表示修正はPC／タブレット／スマホ相当4サイズで重なり・横はみ出しなし。
- ユーザーから実装完了後のcommit/push承認あり。確定コミットはObsidianの同日セッションへ記録。

## 2026-09-08 コミット・push承認

- ユーザーからコミット・pushの指示を受領。以下の「未コミット・未push」は作業途中の記録。
- 今回のコミットに前後移動機能・自動テスト・本引き継ぎ文書を含める。送信結果と確定コミット番号はObsidianの9月8日セッションおよびプロジェクトノートに記録する。
- デスクトップ再開時はノートPCからのpush完了記録を確認し、上記手順どおり未コミット変更を保護して同期する。

## 2026-09-08 デスクトップ再開前の注意

- ノートPCのHEADは `30eae5e`。連続再生の前後移動はまだ未コミット・未pushであり、GitHubからpullするだけでは取得できない。
- 未コミット対象: `src/App.tsx`、`scripts/test-autoplay-navigation.mjs`、この引き継ぎ文書。破棄しない。
- デスクトップへ移る前に、ユーザーの承認を得てノートPCで最新remoteとの整合確認・コミット・pushを行う。デスクトップで既に変更がある場合は先に状況を確認し、上書きしない。
- push後、デスクトップ側の正式チェックアウトで `git status --short --branch`、`git remote -v` を確認。未コミット変更がなく正しいmainなら `git pull --ff-only origin main`。分岐・変更があれば止めて確認する。
- 再開後は `node scripts/test-autoplay-navigation.mjs`、`npm.cmd run build`、`npm.cmd run lint` を実行し、画面と実音声で前後移動を確認する。
- Obsidianの `Codex/Projects/English-Typing-RPG.md` と `Sessions/Codex/2026-09/2026-09-08_English-Typing-RPG.md` にも記録。「記録保存」の依頼にはObsidianへの保存を含む。
- バックグラウンド再生は相談段階で未実装。対象端末と画面消灯中も必要かの確認待ち。

## 2026-09-07 連続再生の前後移動（ノートPC・未コミット）

- 「現在再生中」見出しの下に「← 前の用語」「次の用語 →」を追加。再生中のみ操作可能。
- 単語・和訳・例文をまとめた用語単位で移動し、移動先の設定済み再生順の先頭から再生する。シャッフル順を維持する。
- 最初の用語で戻ると聞き直し。最後で進むと終了、リピート時は先頭へ。
- 移動時は古い音声コールバックと待機タイマーを無効化してからキャンセルし、連打による二重進行を防ぐ。
- 再生対象・採点・学習記録・バックグラウンド再生方式は変更していない。
- `node scripts/test-autoplay-navigation.mjs` 成功（実際の再生関数を音声・タイマーの代替でテスト）。ビルド成功、lintエラー0・既存警告1、diffチェック成功。実機の音声聴感と表示は未確認。
- 今回はコミット・push依頼なし。ローカル変更のみ。

最終更新: 2026-08-25

## 2026-08-19 英会話教材の追加（デスクトップPC）

- 教材選択に「英会話」を正式追加。
- 「英会話 はじめて」をLevel 1〜3、各72会話（合計216会話）で構成。
- Level 1はあいさつ・短い反応、Level 2は質問・自己表現、Level 3は場面別ミニ会話。
- 各問題に相手の発言、日本語、返答、返答の日本語、話すコツ、会話例を収録。
- 基礎練習では相手と返答を続けて読み上げ、実戦では相手の発言を聞いて返す形式に変更。
- 解答後に返答を聞き直し、声に出してまねできる導線を追加。
- 単純な挨拶中心にせず、聞き返し、意味確認、オンライン通話、意見、予定変更、授業、買い物、飲食店、旅行などの実践表現を重点的に追加。

## 作業対象

- リポジトリ: `https://github.com/tstylelab/english-typing-rpg.git`
- ブランチ: `main`
- 公開URL: `https://en-typing.tsg-gt.com/`

デスクトップPCでは、同じリポジトリの作業フォルダを開き、最初に `git pull origin main` を実行してから再開する。

## 2026-08-20に行ったこと

### 初めて遊ぶ人向けの入口

- トップ画面に「初めて遊ぶ人はこちら」を追加。
- 押すと、次の2つから迷わず開始できる案内を表示する。
  - 「文字入力から練習する」: はじめてのタイピング練習を開始。
  - 「文字入力はできる」: おすすめの「英検5級・Level 1・基礎練習」を直接開始。
- 英検級やLevelを自分で選びたい人は、同じ案内から教材選択画面へ進める。
- 既存の「前回の続きから」、教材選択、20問バトル、保存済み進捗は変更していない。
- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- GitHubの `main` へコミット `7963054 Add first-time player guidance` をpush済み。

## 2026-08-19に行ったこと

### 英検4級の問題データ調整

- Level 1: 441問。重複していた `o'clock` を1件削除し、`sir` の日本語訳を明確化。
- Level 2: 200問を維持。抽象的・不自然だった表現を、実際に使える熟語・表現へ置き換え。
- Level 3: 200問を維持。英検3級寄りだった現在完了・関係代名詞・複雑な間接疑問文を、英検4級向けの過去形・直接的な5W1Hなどへ置き換え。
- 各Levelに完全一致の重複がないことを確認済み。

### Level 3の文法ポイント

- 英検4級Level 3の200問すべてに、最も重要な文法ポイントを1つ表示する仕組みを追加。
- 入力前には表示せず、答えた後の「前の問題」欄にだけ表示する。
- バトル終了画面では、「今回の問題と結果」の各問題に表示する。
- 表示内容は「文法の種類」「短い説明」「型」の3点。
- 5W1H、過去形、過去進行形、未来、比較、不定詞、動名詞、助動詞、命令文、会話表現などを英文に応じて出し分ける。
- 英検5級、英検準1級、Level 1・2には表示しない。

## 主な変更ファイル

- `src/data/questionSets/eiken/grade4.json`
- `src/data/questionGrammarPoints.ts`
- `src/App.tsx`
- `docs/HANDOFF.md`

## 重要な設計判断

- このゲームは、過去問を始める前の単語帳・熟語帳と、基本文法の反復・定着を担当する。
- 最終的な試験形式への対応は、公式過去問や過去問集で仕上げる前提。
- 文法説明はゲームのテンポを崩さないよう、一問につき1点だけにする。
- 文法ポイントは答えを入力する前には見せない。
- 問題数はLevel 1が441、Level 2・3が各200のまま維持する。
- 5級と4級で重複する基礎語は、復習として価値があるため残す。

## 確認結果

- `npm.cmd run build`: 成功。
- 英検4級問題データ: JSON読み込み成功。
- 問題数: Level 1 = 441、Level 2 = 200、Level 3 = 200。
- 完全一致の重複: 全Levelで0件。
- 文法ポイント: Level 3の200問すべてに割り当て済み。
- 文法ポイント用ファイル単体のESLint: エラーなし。
- プロジェクト全体のESLintには、既存のReact Hook依存関係に関する警告が1件残る可能性がある。

## デスクトップPCで再開するとき

1. デスクトップPCのCodexで、このリポジトリのフォルダを開く。
2. ターミナルで `git status --short` を実行し、デスクトップ側に未保存の変更がないか確認する。
3. 問題がなければ `git pull origin main` を実行する。
4. 必要なら `npm install` を実行する。
5. `npm run dev` を実行し、表示されたローカルURLを開く。
6. 英検4級Level 3をプレイし、解答後と終了画面の文法ポイントを目視確認する。

## 次に確認したいこと

- 「初めて遊ぶ人はこちら」の案内を、PC・タブレット・スマートフォンで実機確認する。
- 2つの入口から、それぞれタイピング練習と英検5級Level 1の基礎練習へ正しく進めるか確認する。
- PC・タブレット・スマートフォンで、文法ポイントが長すぎず読みやすいか。
- 文法ポイントを読んだとき、プレイのテンポを損なわないか。
- 実際の利用者の反応を見て、説明を短くする必要がある文がないか。

## 2026-08-23に行ったこと

### キーボードつき・はじめてバトル

- 初めてのタイピング練習の次に遊べる「もじバトル」を追加。
- 画面にパソコンと同じ並びのキーボードを常時表示し、次に押すキーだけを光らせる。
- パソコンの実物キーボードと、画面上のキーボードのどちらでも入力できる。
- 時間制限、減点、ゲームオーバーは設けず、間違えても正しく入力済みの部分は消えない。
- 全100問を10問ずつ10段階に分けた。
  1. 1文字のキー
  2. 3文字入門
  3. 3文字のどうぶつ
  4. 3文字の暮らし
  5. 3文字の動き・様子
  6. 4文字入門
  7. 4文字のどうぶつ・自然
  8. 4文字の暮らし
  9. 4文字の動き・身近な物
  10. 5文字の英検5級おためし
- 内訳は1文字10問、3文字40問、4文字40問、5文字10問。重複なし、最長5文字。
- 各10問で既存モンスターを1体倒す。撃破後はボタンかPCのEnterキーで次へ進む。
- 音声再生、絵文字、日本語の意味を表示し、英語学習としても理解しやすくした。
- 既存の通常バトルBGMを再生する。初心者バトルへの画面切り替え直後にBGMが停止していた条件を修正した。
- モンスター撃破時には、既存の通常モンスター撃破用の勝利音源を入力直後に再生する。全100問クリア時は最終クリア用の勝利音源を使う。
- 問題番号と入力途中の文字を端末内に自動保存し、途中でやめても次回は同じ場所から再開する。
- 別のタブやアプリから戻った際と、ゲーム画面内の余白を押した際に、入力欄へフォーカスを戻す。
- 最後まで終わった後は、もう一度遊ぶか、英検5級Level 1の基礎練習へ進める。
- トップの「初めて遊ぶ人はこちら」と、従来のタイピング練習完了画面の両方から入れる。
- 既存の通常バトル、20問バトル、学習記録には影響を与えない独立モード。

### 確認結果

- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- `git diff --check`: 問題なし。
- PC、タブレット、スマートフォン横向きの画面を確認。
- スマートフォン横向きでも、モンスター、問題、入力欄、画面キーボードが同じ画面内で確認できる。
- 間違ったキーを押したときの案内と、その後に正解して次へ進めることを確認。
- 100問、10段階、最初の残り10問の表示を確認。
- 最初の10問を通して、撃破音、撃破画面、「あと0問」を確認。
- 撃破画面でEnterキーを押し、次のモンスターへ進むことを確認。
- 11問目を1文字入力して中断し、再開後に問題位置と入力済み文字が復元されることを確認。
- 別タブを開いて戻った後も入力できることを確認。
- ブラウザのエラー・警告は0件。
- 基本機能はコミット `138d560 Add guided beginner battle mode` としてpush済み。
- 100問への拡張とBGM修正はコミット `bd958dc Expand guided beginner battle` としてpush済み。
- 10問区切り、勝利音、Enter、自動保存、入力復帰の修正はコミット `c775635 Improve beginner battle continuity` としてpush済み。

### 変更ファイル

- `src/data/beginnerBattle.ts`（新規）
- `src/App.tsx`
- `src/index.css`
- `docs/HANDOFF.md`

### 次に行うこと

1. 実際に5歳前後の初心者が遊び、説明なしで光るキーを押せるかを見る。
2. 問題の言葉や10問ごとのテンポが適切かを観察する。
3. 問題がなければコミット・pushする。

## 2026-08-25に行ったこと

### 初めて遊ぶ人向け案内の順番整理

- 作業開始前に `git status --short` と `git pull --ff-only origin main` を実行し、GitHubの `main` が最新であることを確認した。
- 案内画面の上段2枚を、初心者向けの連続した2段階に整理した。
  - 左上: 「はじめてキーボードを使う」からタイピング練習へ進む。
  - 右上: 「タイピング練習を終えた」からキーボードつき・はじめてバトルへ進む。
- その下に「英語学習の本編に進みたい」を配置し、英検5級Level 1の基礎練習へ進むようにした。
- 最下部は「英検の級・Level・英会話を自分で選びたい」とし、教材選択へ進む。
- 見出しを「今の自分に近いものを選ぼう！」、補足を「迷ったら左上から。順番に進めば大丈夫です。」へ変更した。
- PC画面で配置と文章を目視確認し、ブラウザのエラー・警告が0件であることを確認した。
- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- `git diff --check`: 問題なし。
- この案内画面の変更は、コミット `d584e05 Clarify first-time player path` としてpush済み。

## 2026-08-25に行ったこと

### キーボードつき・はじめてバトルの後半100問

- 現在の1～100問目は、内容と順番を変更せず保持。
- 101～200問目に、重複のない新規100語を追加。
- 追加分の内訳は3文字20語、4文字40語、5文字40語。最長5文字。
- かぞく、からだ、食べ物、動物、自然、学校、家、色、動作など、5歳前後でも絵と日本語から意味をつかみやすい単語にした。
- 前半100問は従来どおり大文字で表示し、後半100問の問題はすべて小文字で表示する。
- 画面上のキーボードは後半も大文字のままとし、小文字と大文字キーの対応を自然に覚えられるようにした。
- 100問目で「前半100問 クリア！」を表示し、「つづきの100問へ」と「英検5級へすすむ」を選べる。
- 後半へ10問ごとのモンスター戦、Enter進行、BGM、効果音、入力ガイド、途中保存は継続する。
- 保存形式は問題番号のままなので、既存の進行データと互換性がある。
- 200問すべてで、問題開始時に「単語全体 → 0.3秒の間 → 次に押す1文字」の順で読み上げる。
- 正しい1文字を入力するたびに、次に押すアルファベットを読み上げる。途中再開時は、単語全体の後に未入力の次の文字を読む。
- 入力が早い場合は古い文字の音声を止め、現在の次の1文字だけを読むことで、音声の順番待ちを防ぐ。

### 確認結果

- 全200問、ユニーク200問、重複0、最長5文字を確認。
- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- `git diff --check`: 問題なし。
- PC表示で、100問達成画面、選択ボタン、後半への移動、小文字問題、大文字キーボード、入力後の自動進行を確認。
- タブレットとスマートフォン横向きで、横はみ出しがなく、20ステージ表示と小文字問題が読めることを確認。タブレットでは画面内に全体が収まる。
- ブラウザのエラー・警告は0件。
- 問題開始と「単語とつぎの文字を聞く」ボタンの導線、1文字入力後に次の文字へ進む処理を確認。
- この変更は `Expand guided beginner battle` としてコミット・push済み。

## 2026-08-25 日本語入力中でも英字を直接受け取る修正

- 次のモンスターへ進んだ際、Windowsの日本語入力状態や入力欄の作り直しによって、英字を入力できなくなることがある問題へ対応した。
- PCの物理キーボードでは、画面の入力欄が選択されていなくても、押したA～Zキーをゲーム側で英字として直接受け取る。
- 日本語入力が有効なままでも、原則として半角／全角キーを押し直す必要はない。
- 全角英数字や全角記号が入力欄へ渡った場合も、半角へ自動変換して判定する。
- 通常バトル、20問バトル、はじめてバトル、はじめてのタイピング練習へ共通で適用した。
- スマートフォンとタブレットの画面キーボード入力は従来どおり利用できる。
- 各入力画面に「半角／全角の切り替え不要・日本語入力のままでOK」と表示した。

### 確認結果

- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- `git diff --check`: 問題なし。
- 入力欄ではなく音声再生ボタンを選択した状態から英字キーを押し、通常バトルとタイピング練習の入力が進むことを確認した。
- 全角の `Ｄｅｃｅｍｂｅｒ` を入力し、通常バトルで `December` として正解判定されることを確認した。
- Windowsの実際の日本語入力による変換途中の挙動は、実機キーボードで最終確認する。
- 日本語入力対策は、20問バトルのヒント位置修正と合わせてコミット `79a4db0 Improve typing input and versus hints` としてpush済み。

### 20問バトルのヒント位置修正

- 従来はミス回数に応じて単語の先頭から1文字ずつヒントを表示していたため、途中の文字で初めて間違えた場合も、その位置のヒントを得るまで何度も間違える必要があった。
- 入力内容と正解を先頭から比較し、実際に最初に食い違った位置をヒントとして直接表示するように変更した。
- 表示は「5文字目は『s』」のように、位置と正しい文字が一目で分かる形式にした。
- ローカルの20問バトルで「書店 = bookstore」に `bookx` と入力し、1回のミスで「5文字目は『s』」と表示されることを確認した。
- ビルド成功、lintエラー0件、差分チェック問題なし。この変更もコミット `79a4db0 Improve typing input and versus hints` としてpush済み。

## 2026-08-31 Androidタブレットの英語入力開始を改善

- 当初試したパスワード形式は、Pixel TabletのChromeで翻訳確認とパスワード保存確認が表示されたため、直ちに撤回した。
- `email` 向け入力設定もPixel 9aの画面キーボード入力を妨げたため撤回し、Pixel 9aで利用できていた通常の文字入力設定へ戻した。
- Pixel 9aはスマートフォン単体の画面キーボード、Pixel TabletはUSBキーボード接続で利用する前提を明確に分ける。
- 通常バトルと20問バトルに、端末ごとに保存される「USBキーボード」切替を追加した。初回の初期値はOFF。
- OFFではPixel 9aを従来どおり `inputmode=text` に保つ。Android Tabletだけは画面キーボードを英字向けにするため `inputmode=email` を使うが、入力欄の種類は安全な `type=text` のままにする。
- Pixel TabletでUSBキーボードを使うON時は、編集欄ではなくフォーカス可能なゲーム領域を選択する。Android日本語IMEの編集接続を作らず、既存の `KeyboardEvent.code` で物理英字キーを取得する。
- `keydown` を主経路とし、Androidが押下イベントを渡さなかった場合に備えて `keyup` も重複なしの予備経路として追加した。
- ONの保存中にUSBキーボードを外した場合、通常バトルは問題欄、20問バトルは入力欄をタップすると自動でOFFへ切り替わり、画面キーボード入力へ戻る。切替ボタンからも戻せる。
- 「もう一度聞く」、問題切替、スキップ、復習操作の後は、ONならゲーム領域、OFFなら入力欄へフォーカスを戻す。
- 全角英数字で確定された内容は半角へ正規化して受け付け、日本語の変換文字列は誤答として数えず破棄する処理を通常・20問バトルの両方へ揃えた。
- ページ本体は日本語UIなので、文書言語を `lang=ja` に修正し、ゲーム内の日本語・英語教材をChromeに自動翻訳させないため `translate=no` を指定した。
- ローカルChromeで、通常バトルのOFF時は入力欄に `s`、ONへ切り替えた後はフォーカスされたゲーム領域から `o` が入力され、問題を完了できることを確認した。
- 20問バトルのON時も編集欄が非選択、ゲーム領域が選択された状態で物理キー `a` が入力表示へ反映されることを確認した。
- Pixel TabletとPixel 9aの実機確認は、GitHubへpushして本番反映後に行う。

## 2026-08-31 20問バトルの前回設定を保存

- バトル開始に成功した時点で、参加者名・参加人数・参加者ごとの教材とLevel・得点倍率・出題方法を端末のローカル保存へ記録する。
- 次に20問バトルの設定画面を開いたとき、最後に実際に使用した設定を初期表示する。入力途中の空欄や開始できなかった設定は保存しない。
- 保存値は読み込み時に1〜5人、名前20文字、存在する教材・Level、0.70〜1.40の有効な得点倍率、存在する出題方法へ正規化し、壊れた値は安全な初期値へ戻す。
- 学習履歴やプレイヤープロフィールとは分けた端末内設定とし、通常ゲームの記録には影響させない。
- ローカルChromeで2人分の名前・教材・Level・得点倍率・出題方法を変更してバトルを開始し、ページ再読み込み後に設定画面を開き直して、全項目が同じ値で復元されることを確認した。
- ビルド成功、lintエラー0件（既存の依存配列warning 1件のみ）、差分チェック問題なし。

## 2026-09-05 英語タイピングバトル設定画面をコンパクト化

- 重複していた参加者ごとの教材サマリーを削除し、実際に編集する名前・教材・Level・得点倍率を最初の設定項目へまとめた。
- タイトルと開始ボタン、参加者設定と出題方法、採点ルールとランキングをPC幅では横並びにし、余白とボタンの高さを縮小した。
- スマートフォンでは従来どおり縦に積み上がるレスポンシブ配置を維持した。
- ローカルChromeの1536×768相当の表示で、1人・2人設定とも画面全体が縦幅内に収まり、主要設定とランキングまで一度に確認できることを確認した。
- ビルド成功、lintエラー0件（既存の依存配列warning 1件のみ）、差分チェック問題なし。

## 2026-09-05 キーボードつき・はじめてバトルを300問へ拡張

- 既存の1〜200問の内容と順番は変更せず、201〜300問目に重複のない100問を追加した。
- 追加問題はすべて1〜5文字に収め、2文字の短い単語と、従来少なかったQ・J・V・X・Zを含む単語を重点的に配置した。
- 全300問で重複0、最長5文字、アルファベット26文字がすべて1回以上登場することを確認した。
- 100問目に加えて200問目にも区切り画面を表示し、「追加の100問へ」または「英検5級へすすむ」を選べるようにした。
- 300問目を終えたときだけ最終クリア画面へ進み、「300もん クリア！」と表示する。
- ステージ数は20から30へ増加。既存のモンスターを順番に循環させ、追加ステージでも同じモンスターだけに固定されないようにした。
- 保存形式は従来と同じ問題番号のため、1〜200問を進めている既存の途中データをそのまま引き継げる。
- ローカルChromeで、初回案内に「全300問」「100問ごとに続けるか選べます」と表示されること、開始画面が「ステージ 1 / 30」になることを確認した。
- `npm.cmd run build`: 成功。
- `npm.cmd run lint`: エラー0件。以前からあるReact Hook依存関係の警告が1件。
- `git diff --check`: 問題なし。
- この変更は未コミット・未push。ユーザー確認後にコミット・pushする。

## 2026-09-06 はじめてバトル400問・英検5級の任意キーボードガイド（ノートPC）

### 同期確認と変更範囲

- 正式作業先 `english-typing-rpg-tstylelab` の `main` で作業。開始時は未コミット変更なし。
- `git fetch origin` 後、ローカルと `origin/main` はともに `3a633ec Expand beginner battle to 300 questions`、差分0件。上記9/5の300問拡張は、このコミットで既にpush済みだった。
- 今回は400問への拡張と、任意のガイドを英検5級へ持ち越す導線のみ。HP、採点、通常教材、リスニング・和訳バトル、20問バトルのルールは変更していない。

### はじめてバトル

- 最初の300問は本文・和訳・絵文字・順序をすべて維持。301〜400問に、復習60問と新しい40語を追加した（400種類の新語ではない）。
- 追加分はすべて小文字3〜5文字。3文字20問、4文字49問、5文字31問。絵文字・単語と次のアルファベットの読み上げ・BGMは既存の仕組みを継続する。
- 10問×40ステージ。100・200・300問目で、次の100問またはガイド付き英検5級へ進むことを選べる。400問目が最終クリア。
- 既存の途中保存形式は変更せず、問題番号と入力中の文字をそのまま引き継ぐ。
- 旧版は300問クリア時に途中保存を削除していたため、初問の入力前に「追加100問から遊ぶ（301問目〜）」を表示する。クリア済みかは自動推測せず、本人が選んだ場合だけ301問目から開始・保存する。

### 英検5級のガイド

- 対象は英検5級 Level 1「基礎練習」（`mode=guide`、元からスペルが表示されるコース）だけ。英語を思い出すリスニング・和訳バトルには出さない。
- 初期値は「非表示」。上部の小さな切替で「次のキーが光る」「表だけ」「非表示」を選べる。
- `etyping_battle_keyboard_guides` にプレイヤーID別で保存し、他のプレイヤーは初期値の非表示を維持する。端末内の表示設定であり、既存の学習記録の転送ファイルには含めていない。
- はじめてバトルの区切り・クリア画面の「ガイド付きで英検5級へ」を選んだ場合は、そのプレイヤーだけ光るガイドを有効化する。
- 実物のキーボードと画面タップの両方で入力できる。大文字も該当英字キーで入力でき、「a lot of」等に必要なスペースキーも用意した。
- ガイド中は既存の物理キー入力処理と非編集領域へのフォーカスを使用し、画面キーボードとスマホ標準IMEの二重表示を避ける。非表示時は通常の入力欄・レイアウトに戻る。USBキーボード設定自体は変更しない。
- PC・タブレットではモンスターを左、問題とガイドを右へ配置。CSSはガイド表示時だけに限定し、非表示時にガイド用の余白を残さない。

### 検証・次の確認

- `npm.cmd run build` 成功。既存の大きなJSバンドル警告のみ。
- `npm.cmd run lint` エラー0件。既存の `beginVersusTurn` の依存配列警告1件のみ。
- `git diff --check` 問題なし。
- テスト専用Chromeで、全400問・40ステージ・既存300問の完全一致、300問目の再開と区切り、Enterで301問目へ進行、入力途中の保存、追加分への直接開始、400問クリアからガイド付き英検5級へ進む導線を確認。
- ガイド切替と再読み込み後の保存、他プレイヤーが非表示になること、英検5級の音声／和訳／複合バトル・Level 2・英検4級にはガイドが表示されないことを確認。
- `March` の大文字、`soy sauce`・`a lot of` のスペースを含めて光るキーを押すだけで正解できること、撃破後Enterで次のモンスターへ進んでもガイドを維持して入力できること、ウィンドウのフォーカス復帰後も入力できることを確認。ブラウザ実行時エラー0件。
- PC 1366×768、タブレット1024×768、スマホ相当844×390／390×844で横はみ出しなし。入力欄とガイドが画面内に収まることを確認。スマホ相当では前の問題の復習カードなど下部情報にはスクロールが必要な場合があるが、情報を削除・折り畳みしていない。
- タッチ操作の模擬テストで1タップ1文字、入力領域が非編集状態であることを確認。Android/iOS実機のIMEやBGM・読み上げの聴感は、実機で最終確認する。
- テストは専用ブラウザの別保存領域で実施し、ユーザーのブラウザの学習記録には触れていない。
- 2026-09-06にユーザーからコミット・pushの依頼を受領。完了コミット番号と同期結果は、Obsidianのプロジェクトノートおよび同日のセッション記録に残す。デスクトップ再開時は未コミット変更を確認し、`main` を安全に同期してからこの節を読む。

## 2026-09-06 トップ画面の本編／タイピング練習の入口を明確化

- 「初めて遊ぶ人はこちら」を「タイピングに慣れていない方へ」に変更。「キーの場所から、ゆっくり練習できます」を補足し、メインの開始ボタンの下へ配置。見た目も補助ボタンとして控えめにした。
- 案内内は「タイピングの練習から始めたい方へ」に変更。文字入力ができる方は本編へ進めると説明。既存の5つの選択肢・閉じる操作はそのまま。
- メインボタンは初回「教材を選んではじめる」で教材選択画面へ、本編開始後は従来の「前回の続きから」で選択中コースの再開へ。
- `etyping_main_game_started_players` にプレイヤーごとの本編開始フラグを保存。出題可能な基礎練習／バトルの開始で記録し、トップ閲覧やタイピング練習だけでは記録しない。
- 旧版のプレイヤーは既存の撃破記録・スコア・累計入力・日別学習記録から再訪と判断。学習記録自体は変更せず、新フラグは端末内の表示設定として扱う。
- ビルド成功、lintエラー0件（既存warning1件）、差分チェック成功。
- テスト専用Chromeで初回の文言と配置・教材選択への遷移・練習だけでは初回のまま・本編開始後と再読み込み後の再開表示・別プレイヤー・旧記録の引継ぎを確認。案内の選択肢を維持し、ブラウザ実行時エラー0件。PC・スマホ相当の表示を確認。
- 2026-09-07にユーザーからこのトップ画面修正のコミット・pushを依頼された。完了番号と公開結果はObsidianのプロジェクトノートと9/7のセッション記録に残す。
- デスクトップ再開時は、そのPCの正式チェックアウトでブランチ・未コミット変更を確認し、安全なら `git pull --ff-only origin main`。分岐や未コミット変更がある場合は破棄せず確認を優先する。同期後はこの節を読む。学習記録・表示設定はブラウザ内のためGitの同期対象ではない。
